# レボファンディング 実装指示書（Codex向け）
版：v1.0（2026-06-13）／ 設計の正本：`revofunding_challenge_page_spec.md`（以下「台帳」）

---

## 0. この指示書の使い方（最初に必ず読む）

- あなた（実装AI）は、この指示書の順序どおりに段階的に実装する。**飛ばさない・先回りしない。**
- 各ステップには【完了条件】と【確認ポイント】がある。完了条件を満たしたら、次に進む前に必ず確認ポイントを自己チェックする。
- 設計の判断根拠が必要なときは台帳の該当章（§番号）を参照する。**指示書と台帳が食い違ったら台帳が優先。**
- **統一感が最優先**。色・サイズ・余白・フォント・角丸は STEP 1 で定義する共通トークンを全ステップで使い回す。各ステップで個別に色やサイズを直書きしない。
- 1ステップ完了ごとに、それまでの全成果物を1つの確認用ページ（`preview/index.html`）にまとめて並べ、見た目の統一を目視確認できるようにする。

---

## 技術前提

- 出力は静的フロントエンド（HTML / CSS / バニラJS）。ビルド不要で開ける形を基本とする。
- データは当面モック（JSONオブジェクト）で動かす。将来 Supabase / スプレッドシートに差し替える前提で、**データ取得部は関数に切り出す**（`fetchProjects()` 等。中身を後で差し替え可能に）。
- ブラウザストレージ（localStorage 等）は使わない。状態はJS変数・メモリで保持。
- レスポンシブ必須。ブレークポイント 768px（台帳§4-2）。
- 画像は 1200×630（1.91:1）を基準（台帳§4-1）。実画像が無い間は同比率のプレースホルダを使う。

---

## STEP 1 — デザイントークン（全実装の土台・最重要）

最初に、全ステップで使う共通の定義を1ファイルにまとめる（`styles/tokens.css` + 必要なら `tokens.js`）。**以降のステップは必ずこのトークンを参照し、色・サイズを直書きしない。**

### 1-1. 色トークン（台帳§14-2 / §14-6）
2層構造で定義する。

種類の色（鮮やか・原色寄り。差し色に使う。くすませない）：
- `--c-kian`（起案者）= #F5333A
- `--c-spark`（スパーク）= #FF7A00
- `--c-boost`（ブースト）= #0A6CF0
- `--c-art`（レボアート）= #8A2BE2
- 認定スパーカー = 金（光沢・グラデーション。後述の金CSS）
- 認定アーティスト = プラチナ（光沢・グラデーション。後述のプラチナCSS）

状態の色（全種共通）：
- `--c-done`（達成）= #1D9E75
- `--c-rush`（追い込み）= #D8420F

各種類色に淡色版（プログレスバーの地・バッジ背景用）も定義（例 `--c-spark-pale`）。

金・プラチナはベタ塗り不可。CSSのlinear-gradient（複数ストップ）＋斜めのハイライト擬似要素で金属の光沢を表現（台帳§14-6）。プラチナは白寄り高明度＋寒色＋鏡面ハイライト（金との差別化）。

### 1-2. サイズ・形トークン
- カード画像比率：`aspect-ratio: 1200/630`
- 角丸：カード `--radius-card: 12px`、ボタン（CTA）はピル型 `999px`
- 影：`--shadow-card: 0 3px 12px rgba(0,0,0,0.07)`
- 左帯：`--bar-width: 5px`（種類色。カード左辺）

### 1-3. タイポ・余白
- 端正な日本語フォントスタック（system-ui系）。見出し・本文・キャプションの3段階のサイズを定義。
- 余白スケール（4 / 8 / 12 / 16 / 24px）をトークン化。

【完了条件】tokens.css に上記すべてが定義され、色見本・金/プラチナ見本を並べた `preview/index.html` で全トークンが目視確認できる。
【確認ポイント】鮮やかか（くすんでいないか）。金とプラチナが別物に見えるか。プラチナが冷たい白銀に見えるか。

---

## STEP 2 — 共通カードコンポーネント（全ページの部品）

台帳§14 全体。1つのカード部品を作り、**データ（種類・状態・中身）を渡すと7種すべてを出し分けられる**ようにする。種類ごとに別コンポーネントを作らない。

### 2-1. カードの骨格（全種共通）
- 左帯（種類色・5px。左辺は角を立て右側を角丸：`border-radius: 0 12px 12px 0`）
- 画像エリア（1200×630・実写真が入る。今はプレースホルダ。左上に種類バッジ）
- 本文（タイトル＋主役1つ＋フッター）
- カード枠：無彩色の細枠＋やわらかい影（左帯だけが種類色）

### 2-2. 種類による出し分け（台帳§14-3 / §14-6）
データの `type` で切り替える：
- `kian`（起案者・赤）：主役＝挑戦件数・巡達成数
- `spark`（スパーク・橙）：主役＝あと◯人 ＋ プログレスバー
- `boost`（ブースト・青）：主役＝あと◯本 ＋ プログレスバー
- `art`（レボアート・紫）：主役＝作品テーマ
- `sparker`（認定スパーカー・金）：主役＝最初の仲間になった回数。画像エリアは金の光沢
- `artist`（認定アーティスト・プラチナ）：主役＝力を貸した分野。画像エリアはプラチナの光沢

### 2-3. 状態による上書き（台帳§12 / §14-2）
データの `phase`（幕）で見た目を変える：
- 通常（幕2-3）＝種類色
- 追い込み（幕4・あと3以下）＝濃橙の枠＋影を強調、数字を濃橙に
- 達成（幕5/完走）＝緑の状態色＋達成バッジ
- ゼロ（幕0）＝スパークのみ特別（「最初の一人になれる」を主役に、数字は控えめ）。ブーストのゼロは特別扱いせず由来タグで補完（台帳§8-3b）

### 2-4. 人カードのDL（台帳§14-4）
`sparker` / `artist` カードは画像として保存（ダウンロード）できるボタンを付ける（本人がバッジとして所有）。実装は後でよいが、UIの場所だけ確保。

【完了条件】1つの `renderCard(data)` 関数（または同等のコンポーネント）が、type 6種 × phase 各状態を、同じ骨格・トークンで出し分ける。preview に全種×主要phaseを並べる。
【確認ポイント】全カードが同一サイズ・同一余白か。左帯だけが種類色で、写真エリアはプレースホルダ（色ベタ塗りでない）か。一覧に並べたとき統一感があるか（虹色でうるさくないか）。

---

## STEP 3 — スパーク個別ページ（最初に公開される最重要画面）

台帳§3〜§8・§11-A・§12。最初の挑戦が必ず通る画面。6幕で表情が変わる。

### 3-1. レイアウト（台帳§4-2）
- PC（≥768px）：画像左 / 数字ブロック右の2カラム
- スマホ（<768px）：画像上 / 数字下の縦積み。進捗と応援者アバターを1行に圧縮

### 3-2. 数字ブロックの3層分離（台帳§8-4・厳守）
- 主役（特大・1つ）：「この組の達成まで／あと◯人」＋「12 / 20人」
- 条件（中・直下）：「1人 10,000円 × 20人 ＝ 1組 200,000円」を1行
- 文脈（小・別枠）：「🔄 2回目の挑戦｜1巡目（20人）達成済み・累計応援者◯人」
- 主役は人数、金額は条件として中サイズで併記（隠さない）

### 3-3. 直列の組表示（台帳§8-3）
- 主役の上に「◯組目を募集中 ｜ 全◯組」
- 進捗バー下に組チップ（1組目 達成✓／2組目 募集中／3組目 待機）

### 3-4. 用語ルール（台帳§11-A・厳守）
- FVに「スパーク」「巡」の語を出さない。「🔄 2回目の挑戦」「最初の仲間を募集中」と機能で表記。「旗」は可
- FVに「15人」を出さない（実数20人のみ）
- CTA：メイン「この挑戦の、最初の仲間になる」（固定・残人数を含めない）／サブ「応援の使い道を見る」

### 3-5. 6幕の出し分け（台帳§12）
現在の参加人数 n（モックデータ）で表示を変える関数 `renderSparkPhase(n, target)`：
- n=0：幕0（最初の一人になれる・数字控えめ・コーラル濃）
- n=1：幕1（1人目を祝福）
- 2≦n<target-3：幕2-3（あと◯人・応援者の顔）
- n≧target-3：幕4（あと3→2→1で濃橙が濃くなる・枠と影強調・煽り文句は使わない）
- n=target：幕5（あなたが達成させた・緑・次の組/巡の予告）

### 3-6. FV下のセクション
- 「このプロジェクトのポイント」3点（台帳§6-5・本文20%対策）
- 本文（6W3H）
- ティザー：「🔒 仲間が集まると、この挑戦の『旗』の募集が始まります」（台帳§8-1。詳細は書かない）

【完了条件】モックの n を 0→1→中盤→あと2→達成 と変えると、FVの主役・色・CTAが幕ごとに切り替わる。PC/スマホ両方で破綻しない。
【確認ポイント】STEP1のトークン・STEP2のカードと色/サイズが一致しているか。3層分離が守られ主役が1つに見えるか。スパーク・巡・15人がFVに出ていないか。

---

## STEP 4 — 一覧ページ（広場）

台帳§9。STEP2のカードを並べる。

- 同一ページに上段「🔥 最初の仲間を探している挑戦」（スパーク）／下段「🚩 旗を立てて広げたい挑戦」（ブースト）を併置
- グリッド：PC3〜4列・タブレット2列・スマホ1列
- 並び替えボタン：達成間近 / 新着 / 巡が多い（フロントのJSで並替。`sortProjects(key)`）
- 各カードは§12の幕を反映（モックデータの phase で出し分け）

【完了条件】モックの複数プロジェクト（異なる幕・種類）がグリッドに並び、並び替えボタンが効く。スパーク/ブーストが上下に分かれて表示。
【確認ポイント】幕の違うカードが混在して「生きた広場」に見えるか。STEP2のカードがそのまま使われているか（一覧用に作り直していないか）。

---

## STEP 5 — 応援の地図（一覧の下）

台帳§9（地図）・§1.21。レボアート既存の地図機能を転用する想定。無ければ標準地図ライブラリで実装。

- 三層：①色分け（赤/橙/青）の【のべ数】を都道府県・市町村にカウント表示（コロプレス）②希望者の店舗をGoogleマップピンで正確表示 ③総数カウンター
- デフォルト全員表示＋オプトアウト（参加者がチェックで地図から外れる）
- プライバシー明示は攻めのメッセージに：「応援を、地図にしよう。」＋数字連動メッセージ（少数時「最初の応援者になろう。ここから地図が始まる」／成長後「応援は、もうこんなに集まっている。もっと増やしていける」）
- 最小単位ルール：市町村1〜2件は都道府県に丸める
- カード（既定）／地図の表示切替。ピンを開くと共通カードのミニ版

【完了条件】モックのエリア別のべ数が地図に色分け表示され、総数カウンターが出る。表示切替が動く。
【確認ポイント】数字が少ないときも前向きメッセージで白けないか。色分けがSTEP1トークンと一致しているか。

---

## STEP 6 — ブースターページ ＆ S2共創ページ（スパーク達成後）

スパーク達成後にしか見られない＝STEP3公開後の猶予で実装可（台帳§15）。

### 6-1. S2共創ページ（台帳§2 S2）
- 「準備中」にしない。「いま◯人で話し合っています」を見せる共創の舞台
- 活動報告スペース（ミーティングの様子・決まったこと・旗のデザイン案）
- S2→S3は自動でなく、内容決定後に入稿して開始

### 6-2. ブースターページ（台帳§8-1・§8-3b）
- 順序厳守：①FV＝ブースト（旗◯本/◯本・由来タグ一行・CTA「旗を立てる」）即見える → ②旗の三択（着る/飾る/贈る）→ ③由来の記念ブロック「この挑戦は、◯人の最初の仲間から始まりました」→ ④本文
- スパーク達成を先頭に置かない（本題前離脱を防ぐ）
- 主役は旗の本数。ブーストにも6幕（旗0本は特別扱いせず由来タグで補完）

【完了条件】ブーストFVが最上部、由来は下に配置。旗本数で6幕が動く。S2は活動報告が流せる。
【確認ポイント】STEP2/3と色・サイズ・カードが一致。CTAが「旗を立てる」。

---

## STEP 7 — 認定アーティストページ ＆ レボアートページ

共通カードに乗る（台帳§14-4）。交渉の進捗で並行実装可。

- 認定アーティスト一覧：プラチナのカードを並べる。本人DL対応
- レボアート一覧：紫のカード。作品テーマを主役に
- 仮カードでも公開可（骨格に情報を流し込むだけ）

【完了条件】両ページがSTEP2のカードで構成され、preview に並ぶ。
【確認ポイント】プラチナ/紫がトークン通りか。Lab全体でカードの言語が統一されているか。

---

## STEP 8 — データ層の差し替え準備

- 全ステップでモックを使った `fetch系関数`（fetchProjects / fetchSupporters / fetchMapCounts 等）を1ファイルに集約
- 参加記録のデータ構造：`project_id × round（巡）× group（組）× supporter_key` で一意（台帳§12-2）。round・group 列を最初から持つ（後付けは高コスト）
- 自動遷移：参加記録処理の最後に「n が目標到達か」を判定し達成フラグ＋次レコード生成（台帳§12-1。手動操作なし）

【完了条件】データ取得が関数化され、モックを本番ソースに差し替える箇所が1ファイルに集約。
【確認ポイント】round/group列が最初から入っているか。

---

## 全体の確認原則（毎ステップ共通）

1. STEP1トークンを使っているか（色・サイズ・余白を直書きしていない）
2. STEP2カードを使い回しているか（ページごとにカードを作り直していない）
3. PC/スマホ両方で破綻しないか（768px）
4. 用語ルール：FVにスパーク・巡・15人を出していないか
5. 主役は1つに絞れているか（3層分離）
6. preview/index.html に全成果物が並び、見た目が統一されているか

## 進め方
- STEP順に実装し、各STEP完了時に preview を更新して統一感を確認してから次へ。
- 不明点・設計判断が必要な箇所は台帳の該当§を参照。台帳に無ければ実装を止めて確認を求める（推測で仕様を作らない）。